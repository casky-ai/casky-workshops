--- ad-service-accounts.json ---
{
  "domain": "bellwood.local",
  "audit_area": "AD hygiene — service accounts",
  "correlates_to_tailgate_act": 3,
  "findings": [
    {
      "id": "AD-1",
      "title": "SPN-registered service accounts with passwords never rotated",
      "correlation_label": "exploited",
      "correlation_note": "svc-sql01, svc-web01, svc-backup all appear in Act 3's Kerberoasting TGS burst — see .pristine/ad/kerberos-events.txt and spn-listing.txt.",
      "accounts": ["svc-sql01", "svc-web01", "svc-backup"],
      "detail": "All three have SPNs registered and passwords last set 900+ days ago — Kerberoastable by any authenticated domain user with no elevated access required.",
      "recommended_fix": "Rotate service account passwords on a schedule (or migrate to gMSA where possible) and use long, random passwords resistant to offline cracking."
    },
    {
      "id": "AD-2",
      "title": "Service account with Kerberos pre-authentication disabled",
      "correlation_label": "probed, not confirmed exploited",
      "correlation_note": "svc-legacyapp is equally exposed (AS-REP roastable) but ad/kerberos-events.txt found no matching 4768-without-preauth event in the incident window — this exposure was not confirmed as used by the attacker, unlike AD-1.",
      "accounts": ["svc-legacyapp"],
      "detail": "'Do not require Kerberos preauthentication' is set, meaning an AS-REP ticket can be requested and cracked offline with no valid credentials at all.",
      "recommended_fix": "Re-enable Kerberos pre-authentication unless there is a specific, documented legacy-application requirement; if required, isolate the account's permissions to the minimum needed and rotate its password."
    }
  ]
}

--- email-security-policy.json ---
{
  "domain": "bellwoodlogistics.com",
  "audit_area": "email/gateway hygiene",
  "correlates_to_tailgate_act": 1,
  "correlation_label": "exploited",
  "correlation_note": "This is the precondition that let the Act 1 phishing message through — see .pristine/mail-gateway.log's SPF_SOFTFAIL/POLICY_DECISION lines.",
  "dns_records": {
    "SPF": "v=spf1 include:_spf.bellwoodlogistics.com ~all",
    "DMARC": "v=DMARC1; p=none; rua=mailto:dmarc-reports@bellwoodlogistics.com",
    "DKIM": "selector1._domainkey.bellwoodlogistics.com present, valid"
  },
  "findings": [
    {
      "id": "EMAIL-1",
      "title": "DMARC policy is p=none (monitor-only)",
      "detail": "DMARC is published but set to take no enforcement action on failing mail — spoofed/lookalike-domain mail is reported, never quarantined or rejected.",
      "recommended_fix": "Move to p=quarantine, then p=reject, after confirming legitimate senders pass alignment."
    },
    {
      "id": "EMAIL-2",
      "title": "SPF uses ~all (soft-fail) instead of -all (hard-fail)",
      "detail": "A soft-fail result is informational only for most mail gateways' default policy thresholds — it does not, by itself, block delivery.",
      "recommended_fix": "Move to -all once all legitimate sending sources are enumerated in the SPF record."
    },
    {
      "id": "EMAIL-3",
      "title": "No attachment sandboxing/detonation for macro-enabled Office files",
      "detail": "The gateway's macro scan is signature-based only (see mail-gateway.log's ATTACHMENT_SCAN line: 'engine=legacy-av-sig no-match-known-family') — no dynamic detonation is performed on suspicious-but-unmatched macros before delivery.",
      "recommended_fix": "Add a sandboxing/detonation stage for macro-enabled attachments ahead of the signature engine."
    }
  ]
}

--- network-segmentation.json ---
{
  "environment": "bellwood.local internal network",
  "audit_area": "network segmentation",
  "correlates_to_tailgate_act": 2,
  "correlation_label": "exploited",
  "correlation_note": "This is the precondition that let Act 2's lateral movement reach FILESVR01 and JMP-ADMIN01 from a VPN-assigned user address — see .pristine/network/netflow.txt and smb-rdp-events.txt.",
  "subnets": [
    { "cidr": "10.20.14.0/24", "role": "user/VPN-assigned", "vlan": "VLAN-USER" },
    { "cidr": "10.20.1.0/24", "role": "internal servers (file, admin/jump, domain controller)", "vlan": "VLAN-USER" }
  ],
  "findings": [
    {
      "id": "NET-1",
      "title": "No VLAN/ACL boundary between the user subnet and the server subnet",
      "detail": "Both 10.20.14.0/24 and 10.20.1.0/24 are on the same VLAN (VLAN-USER) — a VPN-assigned user address can reach server-subnet hosts with no intervening firewall/ACL decision point.",
      "recommended_fix": "Place internal servers (FILESVR01, JMP-ADMIN01, DC01) on a separate VLAN with an ACL that denies inbound SMB/RDP from the user subnet by default, allowing only named jump paths."
    },
    {
      "id": "NET-2",
      "title": "SMB (445/tcp) unrestricted between the user and server subnets",
      "detail": "No firewall rule restricts SMB access from user-subnet hosts to server-subnet hosts — any authenticated session can reach ADMIN$/C$ shares on any server-subnet host.",
      "recommended_fix": "Restrict inbound SMB on server-subnet hosts to a named admin-workstation allowlist."
    },
    {
      "id": "NET-3",
      "title": "No lateral-movement detection/alerting configured",
      "detail": "New host-pair connections and off-hours SMB/RDP sessions (as seen in Act 2) generate no alert — they were only visible after the fact via manual netflow review.",
      "recommended_fix": "Alert on first-time-seen internal host pairs and off-hours admin-share/RDP sessions."
    }
  ]
}

